<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.learing_platform">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".objects.Userinfo"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:replace="android:icon"
        android:requestLegacyExternalStorage="true">

        <activity android:name=".ui.home.SearchResultActivity"></activity>
        <activity android:name=".ui.home.SearchActivity" />
        <activity android:name=".ui.blog.BlogActivity" />
        <activity android:name=".ui.blog.DeliverActivity" />
        <activity android:name=".ui.home.KnowledgeActivity" />
        <activity android:name=".ui.personal.AboutActivity" />
        <activity android:name=".ui.personal.ChangePwdActivity" />
        <activity android:name=".ui.personal.SettingsActivity" />
        <activity android:name=".ui.personal.EditInfoActivity" />
        <activity android:name=".RegisterActivity" />
        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="adjustPan|stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 注意包名 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.chen.lister.testchangeicon.fileProvider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths"/>
        </provider>

    </application>



</manifest>